#include <string.h>
#include <stdio.h>

int main(void)
{

    if (strcmp("Ficção", "Ficção") == 0)
    {
        printf("iguais\n");
    }
    else
    {
        printf("diferentes\n");
    }

    return 0;
}